# Core ontology dependencies
rdflib>=6.0.0
owlready2>=0.45

# AWS monitoring dependencies
feedparser>=6.0.0

# ArangoDB integration dependencies
# Note: Install ArangoRDF from local clone: pip install -e ~/code/ArangoRDF
# arangordf>=0.1.0
python-arango>=7.5.0

# AWS Ontology dependency (public repository)
# Install with: pip install git+https://github.com/ArthurKeen/AWS_Ontology.git

# Optional dependencies for testing
# Install with: pip install -r requirements.txt
# For reasoning tests (optional):
# owlrl>=6.0.0 