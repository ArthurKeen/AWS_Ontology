# Core ontology dependencies
rdflib>=6.0.0
owlready2>=0.45

# AWS monitoring dependencies
feedparser>=6.0.0
requests>=2.28.0

# Scheduling dependencies (automation/schedule_monitoring.py)
schedule>=1.2.0

# ArangoDB integration dependencies
arango-rdf>=2.0.0
python-arango>=7.5.0

# SHACL validation
pyshacl>=0.25

# NOTE: pyproject.toml is the canonical dependency manifest (use `uv sync`).
# This file is kept for pip-based installs and the Docker image.
# Dev tools (pytest, ruff, coverage, pre-commit) live in pyproject's dev group. 